# Demo repository rules

Only modify `src/auth/**` for this bug fix.

Do not modify `.github/workflows/**`.

Do not modify `.agent-policy.yml`.

Before opening a pull request, run `node --test`.

AI-assisted contributions must disclose AI assistance in the pull request description.
